## What's changed

First working release. The packages previously on nuget.org were `1.0.0.1-beta1` placeholders —
scaffolding with no usable binding in them — so treat this as the initial release rather than an
upgrade. Everything below is new.

Android binds [WebRTC-Android-SDK][android-sdk] at tag `v2.17.2`, built from source. iOS binds
[WebRTC-iOS-SDK][ios-sdk] at commit [`47e2b02`][ios-commit], dated 2025-11-14, which is the tip of
`master`. Mac Catalyst binds that same commit against a Catalyst build of libwebrtc — see below.

**The package version follows the Android SDK, not the iOS one**, because the iOS SDK does not
have a version to follow. Upstream publishes none for it: the repository has one tag (`v2.10.0`,
2020), no GitHub releases, no CocoaPods pod, `MARKETING_VERSION = 1.0` in the Xcode project, and
Ant Media's documentation is versioned by *server* release rather than by SDK. A commit is
therefore the only precise way to say which iOS code you are getting, and `2.17.2` should be read
as "the Android SDK line, plus iOS master as of the date above" rather than as a claim that both
sides are at 2.17.2.

## Packages

| Package | What it is |
| --- | --- |
| `AntMedia.Net.Maui` | MAUI video view, handlers, and the Android `Activity` plumbing |
| `AntMedia.Net` | The cross-platform client: `IAntMediaClient`, options, events, async |
| `AntMedia.Net.Android` | The raw binding, `net8.0-android34.0` / `net9.0-android35.0` / `net10.0-android36.0` |
| `AntMedia.Net.iOS` | The raw binding, `net8.0-ios18.0` / `net9.0-ios18.0` / `net10.0-ios26.0` |
| `AntMedia.Net.Mac` | The raw binding for Mac Catalyst, `net8.0`/`net9.0-maccatalyst18.0` / `net10.0-maccatalyst26.0` |

```
dotnet add package AntMedia.Net.Maui    # MAUI apps
dotnet add package AntMedia.Net         # everything else
```

Each pulls in the one below it, so one reference is enough.

A fourth component, when present, belongs to this repository and increments when the bindings or
packaging change while the native SDKs stay put — `2.17.2.1` binds the same Ant Media code as
`2.17.2`.

## One API across both platforms

`AntMedia.Net` presents the same client on Android and iOS — `IAntMediaClient`, ordinary .NET
events, and awaitable `PublishAsync`/`PlayAsync` that complete when the server confirms:

```csharp
var client = new AntMediaOptions { ServerUrl = "wss://…/websocket" }.CreateClient();
client.SetLocalView(LocalView);
await client.PublishAsync("my-stream");
```

That layer exists because the bindings underneath look nothing like each other, and writing an app
against them directly means roughly 300 lines of per-platform adapter first. The sample publishes
and plays with none.

The raw bindings remain available for anything the cross-platform API does not cover.

## Why iOS needs a facade

**There is nothing usable to bind on iOS.** WebRTCiOSSDK is written in Swift and
exposes almost nothing to Objective-C: its generated header declares exactly two `@objc` classes,
`AntMediaClient` (an `init` and two delegate callbacks) and `Config` (an `init`). `setOptions`,
`publish`, `play`, `AntMediaClientDelegate`, `AntMediaClientMode` — all Swift-only, and therefore
invisible to a .NET binding.

So `AntMedia.Net.iOS` binds an **`@objc` facade this repository maintains and compiles into the
framework**, not the SDK's own surface. Most apps will not touch these types directly — they are
what `AntMedia.Net` is built on:

| Type | What it is |
| --- | --- |
| `AMSClient` | the client — `SetWebSocketServerUrl`, `InitPeerConnection`, `Publish`, `Play`, `Stop`, `SetLocalView`, `SetRemoteView`, camera/mic/zoom control, data channel |
| `AMSClientDelegate` | 20 optional callbacks — connect, publish/play started and finished, track added and removed, data received, errors |
| `AMSMode` | `Join`, `Play`, `Publish`, `Conference`, `Unspecified` |
| `AMSStreamInformation` | a rendition reported by `GetStreamInfo` |

The facade lives in [`native/ios/Facade`](../../native/ios/Facade) and is the file to change when
you need something it does not expose yet.

## Namespaces

```csharp
using AntMedia.WebRTC.Api;   // Android: IWebRTCClient, WebRTCClientBuilder, IWebRTCListener
using Org.Webrtc;            // Android: SurfaceViewRenderer, VideoTrack, PeerConnection …
using AntMedia.Net.iOS;      // iOS: AMSClient, AMSClientDelegate, AMSMode
```

The Android Java package is `io.antmedia.webrtcandroidframework`, which the binding generator would
map to `IO.Antmedia.Webrtcandroidframework` — a mouthful that reads nothing like the package you
installed. It is mapped to `AntMedia.WebRTC` instead, sub-namespaces following (`.Api`, `.Core`,
`.Websocket`).

`Org.Webrtc` is deliberately left as-is: it is what the WebRTC project calls itself everywhere, so
it is the name to search for, and every Android WebRTC example online spells it that way.

## Known gaps

**iOS: members typed in Google WebRTC terms are not bound.** `getStats`, `onStats`,
`registerStatsListener`, `setDegradationPreference`, `deliverExternalPixelBuffer`,
`getLocalVideoTrack` and `getLocalAudioTrack` are all absent, because binding them would force
`AntMedia.Net.iOS` to also bind libwebrtc's entire Objective-C surface. `trackAdded` and
`trackRemoved` are surfaced as a track id and kind (`"audio"`/`"video"`) rather than
`RTCMediaStreamTrack`. Video rendering is unaffected — `SetLocalView`/`SetRemoteView` take a
`UIView`.

**Android: one listener member is renamed.** `IWebRTCListener.onNewVideoTrack` is overloaded in
Java, and both overloads generate the same event handler name, which does not compile. The
two-argument overload binds as **`OnNewVideoTrackWithId(VideoTrack, string)`**; the one-argument
overload keeps `OnNewVideoTrack(VideoTrack)`.

**Mac Catalyst streams, on Apple Silicon, without external audio injection.** `AntMedia.Net.Mac`
binds the same Ant Media commit through the same `@objc` facade as iOS; `AntMedia.Net` and
`AntMedia.Net.Maui` resolve to it for `maccatalyst` target frameworks, and the client API is
identical on all three platforms.

What differs is the libwebrtc underneath. Ant Media links a *customised* one — their framework
exports `RTCAudioDeviceModule`, stock builds do not — and publishes it for iOS only, without the
fork's source, so no Catalyst slice can come from it. The Mac build therefore links a stock
community libwebrtc that ships `maccatalyst` and compiles a small shim beside the facade to supply
the fork-only API their Swift code calls. Ant Media's own source is not patched, and the iOS and
Android packages are untouched.

Two consequences:

- **Apple Silicon only.** Ant Media picks its renderer with `#if arch(arm64)` — Metal on arm64,
  OpenGL otherwise — and Mac Catalyst has no OpenGL, so there is no `maccatalyst-x64` slice to
  ship. Set `<RuntimeIdentifiers>maccatalyst-arm64</RuntimeIdentifiers>`.
- **External audio injection is unavailable.** That is the one thing `RTCAudioDeviceModule` does
  that the shim cannot stand in for; it logs and drops the sample. Camera capture, microphone
  capture, publish and play all work.

Remember the two entitlements a sandboxed Catalyst app needs to capture —
`com.apple.security.device.camera` and `.audio-input` — alongside the usual `NSCameraUsageDescription`
and `NSMicrophoneUsageDescription`. Without them publishing succeeds at every signalling step and
sends no media, which is a confusing way to find out.

**Live-server coverage is Android-only.** CI publishes a real stream to an Ant Media Server
container from an Android emulator, which exercises signalling, SDP, ICE and the server accepting
the broadcast. The Apple equivalents are not automated: GitHub's macOS runners have no Docker, so
there is nowhere to run the server next to them, an iOS simulator has no camera to publish from,
and the Mac Catalyst runner has neither a camera nor anyone to answer the permission prompt. Both
Apple smoke tests take an `ANTMEDIA_TEST_SERVER` environment variable and run the live check when
it is set, so the same coverage is one command away locally — see `docs/BUILD.md`.

## Deprecated package IDs

These were published as `1.0.0.1-beta1` placeholders and are **not** part of this release. Nothing
depends on them and they will not be updated:

- `AntMedia.Net.WebRTC.Dependency`, `.Android`, `.iOS`, `.Mac`

The WebRTC dependency is no longer a separate package: `AntMedia.Net.iOS` and `AntMedia.Net.Mac`
each ship the `WebRTC.xcframework` they were built against, beside the binding assembly, and the
Apple SDK links it into whichever app references the package.

`AntMedia.Net.Mac` was on that list in earlier drafts of these notes. It is now a real package —
see the Mac Catalyst section above.

[android-sdk]: https://github.com/ant-media/WebRTC-Android-SDK
[ios-sdk]: https://github.com/ant-media/WebRTC-iOS-SDK
[ios-commit]: https://github.com/ant-media/WebRTC-iOS-SDK/commit/47e2b0201959f56f26096a00edd7277d2cb87449
